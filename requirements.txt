fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx>=0.28.1,<1.0
pydantic==2.9.2
python-dotenv==1.0.1
openai>=1.57.0
anthropic>=0.45.0
google-auth>=2.48.1,<3.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.147.0
# Sync CMS → Google Sheets (app/sheets_sync.py).
gspread==6.1.4
sqlalchemy==2.0.35
pyyaml==6.0.2
# CMS
jinja2==3.1.4
python-multipart==0.0.12
itsdangerous==2.2.0
passlib==1.7.4
# Fijado a 4.x porque passlib 1.7.4 no es compatible con bcrypt 5.0+
# (usa bcrypt.__about__.__version__ que fue removido en 4.0).
bcrypt==4.2.1
# Demo Gemini 3.1 Flash Live (paralelo a ElevenLabs).
# Lo usa app/gemini_live_bridge.py (endpoint /gemini-demo).
# Necesita GEMINI_API_KEY como env var en Railway.
google-genai>=1.30.0,<2.0
# WebSockets para FastAPI (lo usa /gemini-demo/ws). uvicorn[standard] ya trae
# websockets, pero lo declaramos explícito por si en el futuro alguien quita
# el extra de uvicorn.
websockets>=12.0
